{
  "name": "bnahack-2026",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "agent": ".venv/bin/uvicorn backend.main:app --reload --port 8000",
    "dev:all": "concurrently -k -n frontend,agent \"npm run dev\" \"npm run agent\"",
    "seed:lists": "node scripts/seed-lists.mjs",
    "clear:lists": "node scripts/clear-lists.mjs",
    "build": "next build",
    "start": "next start",
    "lint": "eslint ."
  },
  "dependencies": {
    "@fal-ai/client": "alpha",
    "@fal-ai/server-proxy": "alpha",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.5",
    "eslint-config-next": "^16.3.5",
    "typescript": "latest"
  }
}
